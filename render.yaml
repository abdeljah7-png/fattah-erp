services:
  - type: web
    name: fattah-erp
    env: python
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn erp.wsgi:application --bind 0.0.0.0:$PORT"
    autoDeploy: true
    plan: free